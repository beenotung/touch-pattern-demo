import * as $ from "jquery";
import {isDefined} from "ionic-angular/util/util";
window['jQuery'] = $;
// console.log('window.jQuery', isDefined(window['jQuery']));
export function initGlobal(...dummies:any[]) {
}
